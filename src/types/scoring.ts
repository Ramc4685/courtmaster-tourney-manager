/**
 * Re-exports the ScoringRules type defined via Zod schema.
 */
export type { ScoringRules } from "@/components/admin/tournament/types";

/**
 * Defines the structure for storing scores within a match.
 * Re-exported from entities.ts for clarity, but defined there.
 */
export type { ScoreSet, MatchScores } from "./entities";

